# 学习日志 · 2026-09-20（周日）

> AI Agents Bootcamp **Week 01** 开课日 + 全天讨论
> 这一天的所有产出索引，以及**在讨论中长出来的东西**（课上没有的部分往往更值钱）

---

## 一、课程本身

### 关键发现：同一套 deck，但你的笔记只覆盖了一半

| | |
| --- | --- |
| deck 总页数 | **102 页**（PDF 阅读器右下角） |
| Week 00 笔记覆盖 | **p.1–70**（ACT I–V） |
| ⚠️ **完全空白** | **p.71–102 = ACT VI（The Newsletter）+ ACT VII（Anatomy & Landscape）** |

**猴面包树伏笔的完整判据就在 ACT VII**——追了一整天的那条线，答案在你没有的那 32 页里。
**下次看到 p.70 往后，逐页截图。**

p.1–70 逐页比对结果：**与 Week 00 逐字相同**，页脚仍写 `AI AGENTS · MEETUP · SUPPORTVECTORS` —— 讲师直接复用 9/12 的幻灯片。

### 真增量 ①：记忆三分法（Week 00 明确缺失）

| | 内容 | 例子 | 特性 |
| --- | --- | --- | --- |
| **Episodic** | 带时间地点的经历 | *went to Paris, June 15 2026* | 第一人称、会遗忘 |
| **Semantic** | 脱离情境的事实 | *Eiffel Tower is in Paris* | **"books contain semantic memory"** |
| **Procedural** | know-how | 怎么骑车 | 说不清，只能演示 |

**关系比分类重要**：semantic **从 episodic 蒸馏**而来（consolidation）；procedural **无法外化**。

映射到 agent：episodic = trajectory；semantic = 知识库 / profile；procedural = skills。

⚠️ **蒸馏那一步是风险点** —— 客户说「这次要便宜的」（episodic 事实）→ 模型记成「该客户价格敏感」（semantic 断言）= **未经验证的 consolidation**。

✅ **三层怎么检索和注入，老师说课程后面会讲**（推测在模块 03）。

### 真增量 ②：custom verifier 属于 agent，不属于 harness

```
harness 给：  时机 + 强制机制（拒绝收工、注入追问、有界循环）
你给：        判据本身 —— "对" 是什么意思
```

Hermes 的实现正好印证：通用 verification gate（改了代码没跑测试 → 拒绝收工）+ **`pre_verify` hook**（挂你自己的检查）。

> **harness 拥有 `verifies` 这个动词，但不拥有那个判据。**

### 真增量 ③：下午 lab 换题了

| | Week 00 | Week 01 |
| --- | --- | --- |
| 下午产出 | newsletter | **travel agent + profiles** |

推测原因：newsletter 的 verifier 写不出来（题眼 *cannot be trusted to write alone* 正是此意）。

### 讲法差异（已确认无降级）

口头先给 `agent = LLM + tools + loop`，**下一页 p.21 照旧推翻** → *a decent model with a great harness beats a great model with a bad harness*。

---

## 二、下午 lab · reliability lab（蒸汽机）

**栈**：Phoenix（Arize，`localhost:6006`）+ **Hermes Agent** + **deepseek-v4.1-flash**（故意用小模型）

**三个设计细节**

1. **`skill_view(name="reliability-lab:engine-open")`** = progressive disclosure 现场。skill 运行时才取，命名 `namespace:skill-name`
2. **`fill_boiler(prev_token="cold")`** = 链式 token。**判据做进了函数签名——接口即契约**
3. ⭐ **`engine_journal`** = p.68 的埋伏。journal 是 agent 自述，Phoenix trace 是 harness 的账。**它会说灯亮了，灯没亮。**

**成本结构（现场数据）**：一次 LLM 调用 prompt **23,434** / completion **206**（其中 reasoning 184）。
> ⭐ **输入是输出的 114 倍。agent 贵在 context，不在生成。**

**四条 pipeline**：A 开环 35%（实跑成本 21）→ B 末端 88% → C 逐步 **99.99%**（成本 11）→ D 工具内重试 99.99% 零额外 token。
⭐ **A 的 35% 就是 verifier 算术里那个生成器；B 的 88% = 1 − 0.65⁵。**

---

## 三、概念澄清（讨论长出来的）

| 概念 | 要点 |
| --- | --- |
| **DAG** | 有向**无环**图。agent 恰恰是要打破那个 A（acyclic） |
| **ReAct** | Reason+Act，2022。⭐ 讲师用**过去时**——循环是本质，ReAct 只是 2023 年的写法 |
| **loop vs harness** | loop = 那个环；harness = **拥有并运行**这个环的运行时 |
| **Skill vs Agent** | Skill 是剧本（指令+脚本，当前 Claude 执行）；Agent 是演员（独立实例，自带 context 和工具白名单） |
| **Design by Contract** | precondition / postcondition / invariant。⭐ **verifier = postcondition check**。但 LLM 不遵守契约只会"声称"遵守 → **契约必须被强制检查，不能只被声明** |
| **编译期 vs 运行期** | DSPy/GEPA 是**编译期**——离线产出 prompt。**GEPA 产出的 prompt 是 agent 的一部分，GEPA 本身不是**（gcc 不装在生产服务器上） |
| **指令模板 vs 用户输入** | 模板是函数体（固定、可优化），用户输入是参数（每次不同）。← Golem：嘴里的纸条 vs 许的愿 |
| **tool calling 机制** | 模型只输出 `finish_reason: tool_calls` + 一个结构化请求（那次只有 ~22 token）。**curl 命令只是模型打的字符串**，执行的是 harness |
| **assembles the context** | ⭐ 现场证据：人只打了 `run the engine` 三个词，方括号后面那一大段是 **harness 拼进 user message 的** |
| **prompt injection** | 工具返回的内容模型会读，而它分不清内容和命令。MCP **工具描述也进 prompt**（tool poisoning）。防线在结构：**特权动作不能由被读到的内容触发** |
| **ambient agent** | 信号触发 ≠ 定时触发。**cron 是闹钟，ambient 是哨兵**。自主性最高 → 闸门必须最严 |
| **MARL ≠ LLM 多 agent** | 借的是问题（团队何时胜过独奏者），不是方法 |

---

## 四、数字

```
0.9^10  = 34.9%        每步 90%，十步三分之一都不到
0.99^10 = 90.4%        一个 9 ≈ 多撑一个数量级的步数
0.35^5  = 0.53%        串行
1−0.65^5 = 88.4%       重试五次（闭环反转）
```

**闭环加重试**（p=0.9，10 步）：1 次 34.9% → **2 次 90.4%** → 3 次 99.0% → 5 次 99.99%
⭐ **第一次重试买下了 55.6 个百分点，之后边际收益断崖。** 预算该花在 verifier 准不准，不是堆重试次数。

⚠️ 两个前提：**失败能被检测到**（否则不会触发重试）+ **失败独立**（系统性错误重试一万次还是错）。

⚠️ **pⁿ 是乐观估计**：真实步骤不等概率、不独立，一步错了后面白跑。

---

## 五、上午那份 Ashburn trace（标注版）

**两次未遂 + 四个出厂失败，全部 `exit 0`。**

| | 内容 |
| --- | --- |
| 🔴 未遂 #1 | *"whatever. Let me just report."* —— premature victory |
| 🔴 未遂 #2 | *"or avoid it"* —— **已经把缺陷命名了**还在掂量绕开 |
| 🔴 出厂 #1 | *"could be UTC-ish"* —— 无证据编造解释，从未回头验证 |
| 🔴 出厂 #2 | 时间差异只字未提 |
| 🔴 出厂 #3 | 两源合成一句自信的话，无 provenance |
| 🔴 出厂 #4 | `feels like 81°F` vs wttr 的 26°C≈79°F，静默覆盖 |

> 🔑 **agent 能抓住的是它说出口的失败。出厂的四个，全是它一声没吭的。**

**"这次模型心情好"是巧合吗？** —— 不是巧合也不是能力，**是倾向**。倾向 = 分布 = 有尾巴，**而尾巴上那些次你看不见**（失败静默）。
→ 答案不是选个心情更稳的模型，是**把这件事从模型手里拿走**（TCP over IP：*Nobody fixed the wire*）。

---

## 六、论文

**清单已逐篇核查**（四篇 2026 的预印本全是真的，我本来准备挑错的）→ [`course/reading-list.zh.md`](course/reading-list.zh.md)

**谱系链**（按这个顺序读，不按时间）：

```
ReAct (2022)       轨迹 = 一次性 working memory，用完即弃
   ↓ 失败的教训留不下来
Reflexion          失败 → 文字反思 → episodic memory
   ↓ 经历太多，存不下也找不着
Generative Agents  写入 → reflection 压缩 → 加权检索
   ↓ 文字总结传不了 know-how
Voyager            固化成可执行 skill → procedural memory
   ↓ 每轮该塞什么进 context
MemGPT             把 context 当 RAM 分页管理
```

**本日新增两篇**
- **MemGPT** `2310.08560` —— 原清单漏了，正好管 injection/预算那一步
- **MAST** `2503.13657` —— 多 agent 失败分类（14 种），**结清了 Week 00「team beats soloist」那条欠账**

**Voyager vs Generative Agents 的区别**：一个存「我会做什么」（procedural，代码，**环境是 ground truth**），一个存「我经历了什么」（episodic→semantic，自然语言，**没有 ground truth，只能声称 believability**）。

**下周上课前只读 ReAct**（90 分钟，带五个问题读）。

---

## 七、工程与团队（今天最大的产出）

| 文件 | 内容 |
| --- | --- |
| [`team-learning-path.zh.md`](team-learning-path.zh.md) | **8–11 周团队路线**，六阶段，每加一个动词都由失败驱动 |
| [`phoenix-instrumentation.zh.md`](phoenix-instrumentation.zh.md) | Phoenix 接自己的 loop；⭐ **七动词 → span kind 映射**（EVALUATOR / GUARDRAIL） |
| [`usecase-data-fix-agent.zh.md`](usecase-data-fix-agent.zh.md) | **第一个真实场景**：data fix |
| [`design-proposal-only-agent.md`](design-proposal-only-agent.md) | **中英双语** · agent 只产出 CSV 提案 |

### data fix 场景已确认是真 agent

两个判定都是"是"：**要读散落在几处的文字才能定位哪些行** + **改成什么值需要逐案判断**。

⚠️ 但第二条意味着：

| 判据 | 可机器判定？ |
| --- | --- |
| 执行成功（读回一致、行数、不变量） | ✅ |
| **业务正确性** | ❌ **不行** |

> ⭐ **CSV 不只是安全措施——它是 v1 里业务正确性的唯一 verifier。**

### 关键架构决定

- **工具层先做（MCP），harness 后选** —— 这样阶段 5 同题实测的变量只有 harness 一个
- **只自己写 DB 那个 MCP server**（Jira/Confluence/Slack 有现成的）
- ⭐ **MCP 独立进程 = 独立凭据**：读 server 连库用 `SELECT` 账号，**由 grant 强制，不是靠 prompt**
- **不要自由 SQL**，给参数化修复操作目录
- **审批绑定 `proposal_hash`**，执行时重新校验

---

## 八、⭐ 我说错的，和被纠正的

*（这一节留着，因为纠正比结论值钱）*

| 我说的 | 实际 | 谁纠的 |
| --- | --- | --- |
| "loop 大概 200 行" | **错得最明显。** 那是 happy path；省掉的代码全跑到恢复、并发、超时、副作用 | 外部批评 |
| "dispatches / persists 谁都做得了" | **最糟的一句。** *写入成功、响应丢失、进程重启* —— durable execution 是真难 | 外部批评 |
| "选的标准就一条（verifier）" | 修辞说过头。还要看权限隔离、恢复、审批续跑、可观测性、升级成本 | 外部批评 |
| "记忆、可观测性都要自己写" | 该说的是：**机制可复用，内容和指标是你的** | 外部批评 |
| "厂商 SDK 接口稳定" | 没有这种保证 | 外部批评 |
| "平台等一年" | 「一年」是编的 | 外部批评 |
| "sensor/actuator 画在工具上**不是**阶段上" | **假二分。** 两层互补，且**权限由执行服务强制** | 外部批评 |
| "Stripe Kai actuator 风险接近零" | 它执行代码、持久化文件、访问内部数据。**资金风险低 ≠ 风险低** | 外部批评 |
| "不是营销注水" | 只核实了案例存在，**架构细节是转述搜索摘要，没读原文** | 外部批评 |
| "governor 活的"（那句 *request was not processed*） | **过度解读。** 可能是闸门、额度、或中断——**没有证据，要查 trace** | ⭐ **Rosso** |
| "这个场景 verifier 好写" | **只对了一半**，业务正确性不可机器判定 | ⭐ **Rosso**（两个判定都是"是"） |

**外部批评补进来、我完全没想到的两条：**

1. **agent trace ≠ 业务审计记录** —— 两种记录、两个产生方、两个用途
2. ⭐⭐ **审批必须绑定具体差异**，不能是 `approved=true`。
   *人批了 5% 的佣金，agent 之后改成 15% 却沿用批准* —— 这个洞我没看见

---

## 九、悬而未决

- [ ] **p.71–102（ACT VI / VII）** —— 笔记完全空白，猴面包树判据在里面
- [ ] 讲师有没有点破 p.8 偃师那层反讽（LLM 不可拆 vs 人偶可拆）
- [ ] p.18 Researchers 流派他补没补那一刀
- [ ] self-critique 在什么条件下有效（镜子问题）
- [ ] 那句 *request was not processed* 到底是什么 —— **去 Phoenix 查有没有 LLM span**
- [ ] `engine_journal` 输出 vs Phoenix trace，**并排对照**
- [ ] 自然语言输出的 verifier 怎么写（newsletter 类）

---

## 十、下一步

**下周日上课前**：读 ReAct（90 分钟）

**团队第一周（不写代码）**：
1. 翻过去 3 个月 data fix 工单 → 归类 → **前 5 类覆盖多少%** → **每类耗时分解**
2. 顺便产出 **DB MCP server 的接口清单**（从真实需求倒推）
3. 顺便标：**每类的"对"能不能写成一个查询**（决定未来能不能自动化）
4. 全组读 ReAct
5. 指定一人用 Phoenix 跑 10 个任务，导出 trace

> 💡 第 1 步可能给出意外结论：**前两类占 60%，其中一类根本不需要 agent。**
> 那是好结果——但注意业务价值和学习目标会在这里分叉，提前想好怎么办。

---

## 索引

- [`course/ai-agents-week-01/`](course/ai-agents-week-01/) —— index / delta / notes / summary.en / uploads
- [`course/ai-agents-week-00/`](course/ai-agents-week-00/) —— 主体内容（七动词、四停止规则、失败模式图鉴）
- [`course/reading-list.zh.md`](course/reading-list.zh.md)
- [`team-learning-path.zh.md`](team-learning-path.zh.md)
- [`phoenix-instrumentation.zh.md`](phoenix-instrumentation.zh.md)
- [`usecase-data-fix-agent.zh.md`](usecase-data-fix-agent.zh.md)
- [`design-proposal-only-agent.md`](design-proposal-only-agent.md)
