# 团队学习路线 · 真正学会开发 agent harness

> 建立于 2026-09-20 · 目标**不是**一周出产品，而是让组里长出「我们会做这件事」的能力
> 总时长 **8–11 周**

## ⚠️ 这份文档是什么、不是什么

| | |
| --- | --- |
| ✅ **是** | 一份**学习课程**。产出是**人的能力**，不是产品 |
| ❌ **不是** | 生产架构方案。**不要**把这里的阶段顺序当成产品路线图 |

**生产上的建议是反过来的：复用成熟底座（Deep Agents / LangGraph 或同级），把正式写入权限留在现有业务服务里。**
自己写 loop 是为了**学会读懂别人的 loop**，不是为了长期维护一套自制 harness。

> 这两件事经常被混谈。**"没自己写过就没资格选型"是错的**——
> 正确的说法是：**自己写过的人，选型时问得出好问题。**

---

## 为什么是这个顺序

用课上的话说，你们要的是 **procedural memory**：

> **know-how 说不清，只能演示。**

读十篇论文、选一个框架，得到的是 **semantic memory**（知道有这回事）。
「我们组会做 agent」是 **procedural memory**，**只能靠自己跑坏几次长出来。**

所以全程的核心设计是一条：

> ⭐ **每加一个动词，都必须是被一次真实失败逼出来的。不先看答案。**

这也回避了最常见的失败模式——照着教程搭一套，出事时不知道哪里坏了，因为**没有一行代码是自己疼出来的**。

---

## 阶段 0 · 先读别人的账（1 周）

**不写代码。**

拿现成 harness（Claude Code / Hermes / Deep Agents 任选）跑 **20 个真实任务**，把 trace 全部导出来读。

**要回答的问题**

- 一步 = 什么？trace 里有哪些字段？
- 失败长什么样？——⭐ **重点找 `exit 0` 的失败**
- 哪些信息 harness 记了、哪些没记？

**产出**：每人挑 2 条轨迹，在组会上逐行讲一遍。

**工具**：用 **Phoenix**（Arize，开源，本地 `localhost:6006`）——讲师 lab 用的就是它。
接法见 👉 [`phoenix-instrumentation.zh.md`](phoenix-instrumentation.zh.md)

> **先培养"读账"的眼睛，再谈写。**
> 参考样本：[`course/ai-agents-week-01/delta.zh.md`](course/ai-agents-week-01/delta.zh.md) 里那份标注版 Ashburn trace。

**同期阅读**：ReAct `2210.03629`（理解最小闭环）

---

## 阶段 1 · 裸 loop（1 周）

写那 200 行。**故意不加任何保护**——没预算、没停止规则、没验证、没持久化。

```python
while not done:
    resp = model(messages, tools=schemas)
    if resp.tool_calls:
        for c in resp.tool_calls:
            r = registry[c.name](**c.args)
            messages.append(tool_result(r))
```

**产出**：一个能跑、也能失控的东西。

> ⚠️ 这 200 行是**教具，不是产品估算**。生产系统里省掉的代码会全部转移到恢复、并发、超时、副作用处理上——**代码量不会消失，只会跑到更难的地方去**。这一点要在组里说清楚，避免有人拿它去估工期。

**同期阅读**：SWE-agent `2405.15793`（工具接口设计 / ACI）

---

## 阶段 2 · ⭐ 故意把它跑坏（1–2 周）

**全程最值钱的一段。**

同一个真实任务跑 **50 次**，人工标注每一次的结果，**自己归纳出一份失败分类**。

**产出**：你们自己的失败模式表。

**然后**再去读 MAST（`2503.13657`，14 种失败模式，三类：系统设计 / agent 间错位 / 任务验证），逐条对照：

- 哪些你们自己发现了？
- 哪些漏了？**为什么漏**——是没遇到，还是遇到了没认出来？

> **先自己归纳再看答案，和直接读论文，学到的东西差一个量级。**

**同期阅读**：MAST `2503.13657`（**读在归纳之后**）

---

## 阶段 3 · 一次加一个动词，每个都由失败驱动（3–4 周）

**顺序不重要，触发条件重要。** 只在真的疼过之后才加。

| 先经历这个痛 | 才加这个动词 | 要想清楚的问题 |
| --- | --- | --- |
| 烧掉一笔 token | **enforces** budget | tokens / dollars / wall-clock，三个都要 |
| 看它来回打转 | **stops** · Futility | ⭐「无进展」怎么定义？不是 "no action" 是 "no progress" |
| 它宣称做完了但没有 | ⭐ **verifies** | 判据分三种，见下表。**别只写一种** |
| 出事了查不出原因 | **persists** | ⭐ 三种记录要分开，见下方「审计」一节 |
| 它差点做了件不该做的事 | **escalates** | **阶段 + 工具双层权限**，见下方「权限」一节 |

**每加完一个动词，回头重跑阶段 2 那 50 次，看数字动了多少。**

> 这就是你们自己的 eval 雏形——**不是另起炉灶做的，是长出来的。**

#### ⭐ 判据要分三种，别混成一个

| 类型 | 判什么 | 例子 |
| --- | --- | --- |
| **业务正确性** | 内容对不对 | 费率、币种、生效日期是否符合合同 |
| **执行成功** | 真的写进去了吗 | 目标系统写入后**读回一致** |
| **必须停止** | 什么时候不许继续 | 权限不足、资料冲突、预算耗尽 → 转人工 |

> ⚠️ 两条容易犯的错：
> - **「停止运行」≠「任务成功」** —— 四个出口只有一个是 Success
> - **让另一个 LLM 说"看起来对"，不能替代系统验证** —— 能读回比对的东西，就别问模型

#### 关于「判据先行」的分寸

~~"第一行代码前先交付判据集、工具分类、审计格式"~~ —— **太重了，已修正。**

正确的做法：**开始前写出最小验收标准**（一个场景、10–20 个案例够了），**然后靠原型补齐遗漏**。
判据本来就是在跑坏的过程中长出来的——要求一次写全，反而回到了大设计前置。

**同期阅读**
- verifies → [`course/ai-agents-week-00/ref-harness-engineering.zh.md`](course/ai-agents-week-00/ref-harness-engineering.zh.md)
- persists → MemGPT `2310.08560`、Generative Agents `2304.03442`

---

## 阶段 4 · 读开源 harness 源码（2 周）

**这时候才读。** 因为现在你们有具体问题可问：

- 它的 Futility 怎么判？和我们的比哪个好？
- trace 是 agent 改得了的吗？
- 超预算是抛异常还是静默截断？
- 审批之后怎么续跑？续跑会不会重放副作用？
- 权限隔离做在哪一层？

**读哪两个**

| | 为什么 |
| --- | --- |
| **Deep Agents**（`langchain-ai/deepagents`） | 大概率是你们的候选底座。建在 LangGraph 上，自带 planning / filesystem / subagents / skills |
| **Hermes Agent** | hook 体系最全（27 个生命周期事件 + `pre_verify`），记忆和 skill 是一等公民 |

> **带着自己的实现去对照，而不是从零学。**

**同期阅读**：Scaffold Effect `2607.22585`、AHE `2604.25850`

---

## 阶段 5 · 同题实测（1–2 周）

你们的薄 loop **vs** Deep Agents **vs** 另一个候选，**跑同一批真实任务**。

**比什么**（别只比正确率）

| 维度 | 说明 |
| --- | --- |
| 正确率 | 按你们阶段 3 长出来的判据 |
| **成本** | ⭐ Scaffold Effect 实测：harness 选择可致**每任务 token 数 40 倍**差异，而同模型 pass rate 只差 0–8 个百分点 |
| wall-clock | |
| **故障恢复** | 杀掉进程再起来，业务操作重复了没有 |
| 无效操作数 | |
| **开发工时** | 接一个新工具要多久 |
| 人工介入次数 | |

**到这一步选型自动有答案——而且是数据，不是意见。**

---

## 论文进度总表

| 阶段 | 读 | 编号 |
| --- | --- | --- |
| 0 | ReAct | `2210.03629` |
| 1–2 | SWE-agent | `2405.15793` |
| **2 之后** | MAST — Why Do Multi-Agent LLM Systems Fail? | `2503.13657` |
| 3 · verifies | Addy Osmani《Agent Harness Engineering》精读 | 见 week-00 |
| 3 · persists | MemGPT / Generative Agents | `2310.08560` / `2304.03442` |
| 4 | Scaffold Effect / AHE | `2607.22585` / `2604.25850` |
| 选读 | Voyager（procedural memory） | `2305.16291` |

完整清单与读法见 [`course/reading-list.zh.md`](course/reading-list.zh.md)。

---

## 两条组织上的建议

**1. 别让一个人写完。**
轮换——**写 verifier 的人和写 loop 的人分开**，写完互相拆。

> *Agents reliably skew positive when grading their own work.* —— **这条对人一样成立。**

**2. 每个阶段的产出是"一次演示"，不是一份文档。**
组会上把它跑坏给大家看，比写十页总结有用。**procedural memory 只能靠演示传递。**

---

## 结束时你们手上会有

| 资产 | 绑不绑框架 |
| --- | --- |
| **自己归纳的失败分类**（比论文里的更贴业务） | ❌ 不绑 |
| **真实任务 eval 集 + 判据** | ❌ 不绑 |
| **工具的 sensor / actuator 分类** | ❌ 不绑 |
| **审计账（ledger）格式** | ❌ 不绑 |
| ⭐ **能读懂任何一个 harness 源码的人** | ❌ 不绑 |

> **最后一条才是真正的资产。框架会换，读得懂的人不会过期。**

---

## ⚠️ 三条容易被跳过、但对企业场景是要害

> 这三条属于**生产要求**，不是学习阶段的产物。放在这里是因为阶段 3 会碰到它们的雏形——
> 但真正落实它们的地方是**业务服务和执行边界**，不是 harness。

### 1 · 权限：阶段和工具是**两层**，互补而非二选一

~~"画在工具上，不是画在阶段上"~~ —— **这是假二分，已修正。**

| 层 | 管什么 |
| --- | --- |
| **阶段** | 当前阶段**能拿到哪一组能力** |
| **工具服务** | **每一次调用**逐次检查具体操作 |

```
资料分析   → 只读指定商家的合同与配置
方案生成   → 可写草稿、可跑模拟验证
等待审批   → 不允许发布
审批完成   → 只允许发布【已批准的那一份】变更
```

⭐ **最关键的一条**：**权限必须由执行服务强制落实，不能靠工具名称或 prompt。**

> 单纯拆成「读 agent」和「写 agent」，**如果它们共享同一套高权限凭据，就没有真正隔离。**
> 安全边界要落在**工具层和沙箱层**（Deep Agents 官方文档也是这个要求）。

### 2 · 审计：**agent trace ≠ 业务审计记录**

这是两种东西，之前混为一谈了：

| 记录 | 作用 | 谁产生 |
| --- | --- | --- |
| **Agent trace** | 排查**模型为什么这样行动** | agent runtime / tracing 系统 |
| **业务审计记录** | 证明**谁批准了什么、实际改变了什么** | **执行服务和业务系统** |
| *（notebook）* | agent 自己的工作记忆 | agent，**可改，不可信** |

**真正的要求是两条**：

1. **agent 的身份没有修改 / 删除业务审计记录的权限**
2. **实际写入与审计记录之间有可靠的一致性保障**

业务审计记录至少要关联：**请求人 · 商家 · 变更前后值 · 审批人 · 被批准的变更版本 · 幂等键 · 执行结果**。

> ⭐⭐ **必须防住的洞**：
> **人工批准了 5% 的佣金，agent 之后把参数改成 15%，却沿用之前的批准。**
>
> → **审批必须绑定具体变更内容（diff / 版本哈希），不能只是一个 `approved=true`。**
> → 执行时**重新校验**权限和版本。

### 3 · Prompt injection：分离减少暴露，**执行边界才是拦住它的东西**

外部提交的资料（合同、PDF、工单）进 context 时，**里面的文字是数据不是指令**。

⚠️ 但要清楚：**读写分离解决不了这个问题。**

> 被恶意合同影响的**读取 agent**，照样可以生成一份**有害的变更建议**，交给写入 agent。
> 分离降低的是凭据暴露面，**不是判断被污染的风险**。

真正的控制在执行边界：

| 控制 | 做法 |
| --- | --- |
| 外部文档**只是待分析的数据** | 不因为读了它就获得任何权限 |
| 变更建议用**结构化字段** | 附上合同依据（引哪一条、哪一页） |
| **执行前由代码校验** | 商家范围、费率上下限、合同版本 |
| 高影响操作 | **审批具体差异**；执行时重新检查权限和版本 |

> 通用原则：**特权动作不能由被读到的内容触发。**
> 收件人 / 地址 / 费率 / 命令，必须来自配置或人，**不能来自工具返回**。

---

---

## 📌 与生产路线的分工（避免这份文档被误读）

学习路线和产品架构是**两条并行的线**，不要互相绑架：

| | 学习线（本文档） | 生产线 |
| --- | --- | --- |
| 目的 | 长能力 | 出产品 |
| harness | **自己写薄的**，为了学 | **复用成熟底座**（Deep Agents / LangGraph 或同级） |
| 写入权限 | 不涉及真实系统 | ⭐ **留在现有业务服务里** |
| 时间 | 8–11 周 | 按业务节奏 |

**生产第一版的形态建议**：

> 让 agent 产出 **「带来源的配置草稿 + 测试结果 + 变更差异」**，
> 再接入**已有的**审批和发布机制。**agent 不直接持有发布权。**

这样做的好处是：agent 出错的后果被限制在「草稿质量差」，而不是「配置被改错」。

> **开源 harness 能省掉的是通用执行工程**（loop、重试、持久化、沙箱）。
> **企业业务系统仍然负责授权、交易正确性和审计。**
> 这两句合起来，是支持采用成熟底座的理由，**不是自己写 harness 的理由**。

---

## 相关

- ⭐ [`usecase-data-fix-agent.zh.md`](usecase-data-fix-agent.zh.md) —— **第一个真实场景的设计稿**（data fix）。阶段 2 的 eval 集直接用历史工单
- [`phoenix-instrumentation.zh.md`](phoenix-instrumentation.zh.md) —— 监控接法
- [`course/ai-agents-week-00/summary.zh.md`](course/ai-agents-week-00/summary.zh.md) —— 七动词、四条停止规则、失败模式图鉴
- [`course/ai-agents-week-01/delta.zh.md`](course/ai-agents-week-01/delta.zh.md) —— 记忆三分法、标注版真实 trace
- [`course/reading-list.zh.md`](course/reading-list.zh.md) —— 论文清单与读法
